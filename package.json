{
  "name": "pekdogru-filo-panel",
  "version": "1.0.0",
  "private": true,
  "description": "PEKDOĞRU Filo Kontrol Paneli — statik site + hatırlatma fonksiyonu",
  "scripts": {
    "test": "node scripts/check-syntax.js && node tests/unit.test.js",
    "build": "python3 build.py"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
